[project]
name = "codellama-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nikhil chaudhari", email = "nikhil.inbx@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.18",
    "langchain-community>=0.4.2",
    "ollama>=0.6.2",
    "streamlit>=1.63.0",
]

[project.scripts]
codellama-assistant = "codellama_assistant:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
